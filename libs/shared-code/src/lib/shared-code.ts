/**
 * The placeholder the nx library generator left behind. Nothing but its own spec calls it; it is
 * kept only so the library has an entry point that predates the helpers next to it.
 */
export function sharedCode(): string {
  return 'shared-code';
}
