services:
  backend:
    volumes:
      - backend_vol:/studio-lite-api/packages

  frontend:
    ports:
      - "${HTTP_PORT}:80"
      - "${HTTPS_PORT}:443"
    volumes:
      - ./config/frontend/default.conf.template:/etc/nginx/templates/default.conf.template:ro
      - ./config/frontend/tls/studio-lite.crt:/etc/nginx/ssl/studio-lite.crt:ro
      - ./config/frontend/tls/studio-lite.key:/etc/nginx/ssl/studio-lite.key:ro
