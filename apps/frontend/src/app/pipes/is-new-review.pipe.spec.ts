import { IsNewReviewPipe } from './is-new-review.pipe';

describe('IsNewReviewPipe', () => {
  it('create an instance', () => {
    const pipe = new IsNewReviewPipe();
    expect(pipe).toBeTruthy();
  });
});
