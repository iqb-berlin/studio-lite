import { DataLoadingIsNumberPipe } from './data-loading-is-number.pipe';

describe('DataLoadingIsNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new DataLoadingIsNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
