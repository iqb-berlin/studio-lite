import { TableDataSourcePipe } from './table-data-source.pipe';

describe('TableDataSourcePipe', () => {
  it('create an instance', () => {
    const pipe = new TableDataSourcePipe();
    expect(pipe).toBeTruthy();
  });
});
