import { HasSelectionValuePipe } from './hasSelectionValue.pipe';

describe('HasSelectionValuePipe', () => {
  it('should be defined', () => {
    expect(new HasSelectionValuePipe()).toBeDefined();
  });
});
