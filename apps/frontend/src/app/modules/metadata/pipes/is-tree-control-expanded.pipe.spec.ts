import { IsTreeControlExpandedPipe } from './is-tree-control-expanded.pipe';

describe('IsTreeControlExpandedPipe', () => {
  it('should be defined', () => {
    expect(new IsTreeControlExpandedPipe()).toBeDefined();
  });
});
