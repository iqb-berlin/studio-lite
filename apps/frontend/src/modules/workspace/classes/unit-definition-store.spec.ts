import { UnitDefinitionStore } from './unit-definition-store';

describe('UnitDefinitionStore', () => {
  it('should create an instance', () => {
    expect(new UnitDefinitionStore(0, {})).toBeTruthy();
  });
});
