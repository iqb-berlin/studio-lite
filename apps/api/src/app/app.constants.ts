import {
  JWT_EXPIRES_IN,
  ACTIVE_THRESHOLD_MS,
  PASSIVE_THRESHOLD_MS,
  assertTimeConfig
} from '@studio-lite/shared-code';

assertTimeConfig();

export {
  JWT_EXPIRES_IN,
  ACTIVE_THRESHOLD_MS,
  PASSIVE_THRESHOLD_MS
};
